---
title: Aoteru estate P1 evidence
status: compact-evidence
owner: odysseus
as_of: 2026-08-19
parent: docs/aoteru-estate-execution-contract.md
---

# P1 — estate registry + operator bay

Compact durable record. Do not reread unless a dependency changes.

## Built

- `config/repositories.yaml`, `config/estate.yaml`, `config/models.yaml`,
  `config/memory-sources.yaml` — the four canonical registries (plan §4).
  Symbolic paths only (`${AI_ROOT}`/`${PHD_ROOT}`/`${HOUSEHOLD_ROOT}`), no
  secrets, no hardcoded host paths.
- `scripts/agent` — new top-level CLI (mirrors the existing
  `scripts/odysseus-*` / `scripts/_lib/cli.py` conventions; reuses `emit`/
  `fail`/`common_parser`/`run`, not a new framework). P1 scope only:
  `status`, `where`, `sync`. No `park`/mutation commands exist yet —
  intentional, that's P3.
- Host-local resolution file, generated on first run, never committed:
  `~/.aoteru/config.local.json` (`AI_ROOT` auto-set to this repo's parent
  dir; `PHD_ROOT`/`HOUSEHOLD_ROOT` left `null` rather than guessed).
- Generated bootstraps (disposable, outside git, regenerated by `agent
  sync`): `~/aoteru-bay/CLAUDE.md` (8 lines) and
  `${AI_ROOT}/workspace/CLAUDE.md` (6 lines).

## Gate verification (plan §12, P1)

- **Logical-ID resolution without hardcoded prompt knowledge**: `agent
  status --pretty` run from `/home/agent/projects/odysseus-aoteru` resolved
  both repos actually present on this host (`odysseus` → this repo,
  `odysseus-upstream-lab` → the lab deployment) purely from
  `config/repositories.yaml` + the host-local root vars, and correctly
  reported `misumi`/`obsidian-phd` as `unresolved: <ROOT_VAR> not set` —
  not a guessed or fabricated path. `agent where` identified the cwd as
  the registered `odysseus` repo by the same mechanism. Services (all 6)
  and capability aliases (all 8) correctly report unprovisioned/unbound
  since P2/P7 haven't run.
- **Bootstrap size/content**: `agent sync` wrote both CLAUDE.md files;
  measured 8 and 6 lines respectively (gate: <30). Neither duplicates the
  estate map — both only point at `agent`/MCP commands and defer to each
  repo's own instructions.

Scope note: this session still has only the one host from P0, so the
"both hosts" half of the gate is demonstrated structurally (the resolver
has no host-count assumption baked in — it iterates whatever `estate.yaml`
lists) rather than against two real machines. Re-verify once a second host
is actually registered.

## Gate

- [x] four canonical config registries
- [x] `agent status/sync/where` read-only commands
- [x] generated laptop bay and worker workspace CLAUDE bootstrap
- [x] no mutation/parking implemented

**P1: PASS** (pending fresh-context verification).
