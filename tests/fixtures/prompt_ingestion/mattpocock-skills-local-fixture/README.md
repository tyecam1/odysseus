# Local mattpocock/skills representative fixture

This synthetic fixture represents the reviewed properties needed by the
offline ingestion demonstration: an owner-qualified repository identity, an
MIT licence, and a small software-engineering skill structure. It is not a
network fetch, mirror, or claim about current upstream bytes. The executable
installer described by the external pattern register is deliberately absent
and is never run.
